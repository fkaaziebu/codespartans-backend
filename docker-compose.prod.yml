version: '3.9'
services:
  postgres:
    image: postgres:17
    container_name: postgres-prod
    restart: unless-stopped
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: codespartans-db
    ports:
      - '5432:5432'
    healthcheck:
      test: ['CMD', 'pg_isready', '-U', 'postgres']
      interval: 5s
      timeout: 5s
      retries: 5
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - codespartans-prod-network

  redis:
    image: redis:latest
    container_name: redis-prod
    restart: unless-stopped
    ports:
      - '6379:6379'
    volumes:
      - redis_data:/data
    networks:
      - codespartans-prod-network

  meilisearch:
    image: getmeili/meilisearch:v1.16
    container_name: meilisearch-prod
    restart: unless-stopped
    environment:
      MEILI_MASTER_KEY: 'password'
    ports:
      - '7700:7700'
    volumes:
      - ./meili_data:/meili_data
    networks:
      - codespartans-prod-network

  codespartans-backend:
    image: fkaaziebu/codespartans-backend:latest
    container_name: codespartans-backend-prod
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    env_file:
      - .env
    ports:
      - '4000:4000'
    networks:
      - codespartans-prod-network

networks:
  codespartans-prod-network:
    driver: bridge

volumes:
  postgres_data:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: /mnt/postgres-data

  redis_data:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: /mnt/redis-data
